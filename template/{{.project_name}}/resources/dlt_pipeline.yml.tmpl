resources:
  pipelines:
    testing_dlt_pipeline:
      name: testing_dlt_pipeline
      catalog: {{.default_catalog}}
      target: testing_for_resources_${bundle.target}  
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.py

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
